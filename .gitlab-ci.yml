image: rsmmr/clang

variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - build
  - test

build-job:
  stage: build
  script:
    - ./build.sh

unit-test-job:
  stage: test
  script:
    - ./build.sh
    - ./monkey_test.exe
